# apexverify
High-performance bulk email verification engine with direct SMTP mailbox handshakes, DNS MX resolution, catch-all detection, and disposable email filtering. Features real-time SSE streaming, CSV upload with auto-detect, and one-click clean list export. Runs locally — no API keys, no rate limits.
